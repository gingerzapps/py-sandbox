AddLocationForm: 

<AddLocationForm>: 
  orientation: "vertical"
  search_input: search_box #add id from TextInput to property defined in AddLocationForm() in python file
  search_results: search_results_list 
  BoxLayout:
    height: "40dp"
    size_hint_y: None
    TextInput:
      id: search_box #id is used to reference this elsewhere in KV file
      size_hint_x: 50
    Button:
      text: "Search"
      size_hint_x: 25
      on_press: root.search_location() #calling search_location() function when this button is pressed
    Button: 
      text: "Current Location"
      size_hint_x: 25
  ListView:
    id: search_results_list
    item_strings: []
    
